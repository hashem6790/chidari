# 🚀 راهنمای انتشار و به‌روزرسانی

سه چیز در این نسخه اضافه شده:

1. **ساخت خودکار روی گیت‌هاب** — با هر push، اپ build و تست می‌شود
2. **کلید امضای ثابت** — نسخه تازه روی نسخه قبلی نصب می‌شود
3. **به‌روزرسانی از داخل برنامه** — منو ☰ → درباره → بررسی به‌روزرسانی

---

## 🔑 چرا کلید امضا مهم است؟

اندروید فقط نسخه‌ای را روی نسخه قبلی نصب می‌کند که با **همان کلید** امضا شده باشد.

| حالت | نتیجه |
|---|---|
| همان کلید | ✅ به‌روزرسانی، داده‌ها می‌مانند |
| کلید متفاوت | ❌ خطای نصب، باید حذف و از نو نصب شود |
| بدون کلید (دیباگ) | ❌ هر بار build روی کامپیوتر متفاوت، کلید فرق می‌کند |

تا الان همیشه مجبور بودید `adb uninstall` بزنید — دقیقاً به همین دلیل.

> ⚠️ **کلید را گم نکنید.** اگر گم شود، هیچ راهی نیست که کاربران فعلی
> به‌روزرسانی بگیرند؛ همه باید برنامه را حذف و دوباره نصب کنند.

---

## گام ۱ — ساخت کلید (یک‌بار برای همیشه)

در WSL، داخل پوشه پروژه:

```bash
cd ~/chidari
bash make-keystore.sh
```

یک رمز می‌خواهد. در پایان:
- فایل `release.keystore` ساخته می‌شود (اعتبار ۳۰ سال)
- فایل `keystore.properties` برای build محلی
- یک **رشته base64 طولانی** چاپ می‌شود که برای گیت‌هاب لازم است

هر دو فایل در `.gitignore` هستند و روی گیت نمی‌روند.

📌 **از `release.keystore` پشتیبان بگیرید** — در فضای ابری یا فلش.

---

## گام ۲ — تنظیم Secrets در گیت‌هاب

به این مسیر بروید:

**[Settings → Secrets and variables → Actions](https://github.com/hashem6790/chidari/settings/secrets/actions)** → **New repository secret**

شش Secret بسازید:

| نام | مقدار |
|---|---|
| `SUPABASE_URL` | `https://xgesqzftecltkosjpbbk.supabase.co` |
| `SUPABASE_ANON_KEY` | کلید anon شما |
| `KEYSTORE_BASE64` | رشته base64 که اسکریپت چاپ کرد |
| `KEYSTORE_PASSWORD` | رمزی که وارد کردید |
| `KEY_ALIAS` | `chidari` |
| `KEY_PASSWORD` | همان رمز |

> اگر رشته base64 را گم کردید، دوباره بگیرید:
> ```bash
> base64 -w0 release.keystore
> ```

---

## گام ۳ — build خودکار

پروژه **یک ورکفلو** دارد: `.github/workflows/chidari.yml` با نام **«چی داری؟»**.
خودش تشخیص می‌دهد چه کاری لازم است:

| چه اتفاقی می‌افتد | ورکفلو چه می‌کند |
|---|---|
| push روی `main` یا Pull Request | تست‌ها + APK دیباگ در **Actions → Artifacts** |
| تگ `vX.Y` | تست‌ها + APK **امضاشده** + ساختن Release |
| اجرای دستی بدون شماره نسخه | فقط تست‌ها + APK دیباگ |
| اجرای دستی با شماره نسخه | تست‌ها + APK امضاشده + Release |

نیازی به کار دستی نیست. اگر تستی بشکند، گیت‌هاب ایمیل می‌فرستد و
گزارش تست به‌صورت Artifact بارگذاری می‌شود.

> **چرا یک Job و نه چند تا؟** هر Job جداگانه یعنی یک بار دیگر نصب JDK و
> دانلود وابستگی‌های Gradle. با یک Job، مراحل مشترک فقط یک بار اجرا می‌شوند.

---

## گام ۴ — انتشار نسخه تازه

هر بار که می‌خواهید نسخه بدهید:

### الف) شماره نسخه را بالا ببرید

در `app/build.gradle.kts`:

```kotlin
versionCode = 11        // همیشه +۱
versionName = "2.0"     // شماره‌ای که کاربر می‌بیند
```

> `versionCode` حتماً باید بزرگ‌تر از قبلی باشد، وگرنه اندروید
> نصب را رد می‌کند.

### ب) تگ بزنید و push کنید

```bash
git add -A
git commit -m "نسخه ۲.۰"
git push

git tag v2.0
git push origin v2.0
```

همین. گیت‌هاب خودش:
1. تست‌ها را اجرا می‌کند
2. APK را **با کلید شما امضا می‌کند**
3. یک Release می‌سازد و APK را پیوست می‌کند
4. کاربران از داخل برنامه اعلان می‌گیرند 🎉

پیشرفت را در تب **Actions** ببینید (حدود ۵ دقیقه).

---

## 📱 به‌روزرسانی از داخل برنامه

**منو ☰ → درباره → بررسی به‌روزرسانی**

برنامه هنگام اجرا هم **بی‌صدا** بررسی می‌کند؛ فقط اگر نسخه تازه‌ای باشد پنجره باز می‌شود.

جریان کار:
```
بررسی → نسخه تازه پیدا شد → دانلود (با نوار پیشرفت)
      → نصب‌کننده اندروید باز می‌شود → روی نسخه قبلی نصب
```

بار اول اندروید اجازه **«نصب برنامه‌های ناشناس»** می‌خواهد. برنامه خودش
تشخیص می‌دهد و دکمه‌ی باز کردن تنظیمات را نشان می‌دهد.

---

## ⚠️ نکته مهم: گذار از دیباگ به انتشار

نسخه‌های فعلی شما `ir.chidari.debug` هستند، ولی نسخه انتشار `ir.chidari` است —
یعنی **دو برنامه جدا** از نگاه اندروید.

بنابراین **یک‌بار** باید:
1. نسخه دیباگ فعلی را حذف کنید
2. نسخه انتشار (از Releases) را نصب کنید

از آن به بعد همه به‌روزرسانی‌ها روی همان نصب می‌شوند و دیگر حذف لازم نیست.

---

## 🛠️ build محلی نسخه انتشار

اگر خواستید بدون گیت‌هاب، خودتان APK امضاشده بسازید:

```bash
cd ~/chidari
./gradlew assembleRelease
```

خروجی: `app/build/outputs/apk/release/app-release.apk`

(نیازمند وجود `keystore.properties` و `release.keystore`)

---

## 🔍 بررسی امضای دو فایل

برای اطمینان از اینکه دو APK با یک کلید امضا شده‌اند:

```bash
$ANDROID_HOME/build-tools/34.0.0/apksigner verify --print-certs old.apk | grep SHA-256
$ANDROID_HOME/build-tools/34.0.0/apksigner verify --print-certs new.apk | grep SHA-256
```

اگر هر دو یک مقدار بدهند، به‌روزرسانی کار می‌کند.
