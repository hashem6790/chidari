# راه‌اندازی Supabase و ایمیل (SMTP جیمیل)

برای فعال شدن ثبت‌نام و ورود در برنامه «چی داری؟» این پنج مرحله را انجام دهید.
کل کار حدود ۱۰ دقیقه طول می‌کشد و رایگان است.

---

## ۱. ساخت پروژه در Supabase

1. به [supabase.com](https://supabase.com) بروید و یک حساب بسازید.
2. روی **New Project** بزنید.
3. یک نام (مثلاً `chidari`) و یک رمز پایگاه داده بگذارید.
4. نزدیک‌ترین منطقه (مثلاً `Frankfurt (eu-central-1)`) را انتخاب کنید.
5. چند دقیقه صبر کنید تا پروژه آماده شود.

---

## ۲. برداشتن کلیدها

از منوی سمت چپ: **Project Settings → API**

دو مقدار را کپی کنید:

| مقدار | نمونه |
|---|---|
| **Project URL** | `https://abcdefghijklmno.supabase.co` |
| **anon public** | `eyJhbGciOiJIUzI1NiIsInR5...` (رشته‌ای طولانی) |

> کلید `anon` عمومی است و قرار دادن آن داخل اپ اندروید کاملاً عادی و امن است.
> **هرگز** کلید `service_role` را داخل برنامه نگذارید.

---

## ۳. وارد کردن کلیدها در برنامه

> ✅ **اگر فایل `secrets.properties` را ساخته‌اید، این مرحله انجام شده است.**
> کلیدها در `SupabaseConfig.kt` قرار گرفته‌اند و APK با آن‌ها ساخته شده.
> این بخش را فقط اگر خواستید به پروژه Supabase دیگری وصل شوید بخوانید.

دو مقدار را در فایل **`secrets.properties`** (ریشه پروژه) بگذارید.

اگر این فایل را ندارید، از روی نمونه بسازید:

```bash
cp secrets.properties.example secrets.properties
```

سپس بازش کنید و مقادیر خود را وارد کنید:

```properties
SUPABASE_URL=https://abcdefghijklmno.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5...
```

> 🔒 این فایل در `.gitignore` است و هرگز روی گیت نمی‌رود — برای همین
> کلیدهای شما در ریپوی عمومی افشا نمی‌شوند.

سپس پروژه را دوباره بسازید. تمام.

---

## ۴. تنظیم SMTP جیمیل

Supabase به‌صورت پیش‌فرض ایمیل می‌فرستد ولی **سقف آن روزی ۳ تا ۴ ایمیل** است و فقط برای تست است.
برای استفاده واقعی باید SMTP خودتان را وصل کنید.

### الف) ساخت «رمز عبور برنامه» در گوگل

جیمیل اجازه نمی‌دهد رمز اصلی حساب را در برنامه‌ها بگذارید. باید App Password بسازید:

1. به [myaccount.google.com/security](https://myaccount.google.com/security) بروید.
2. **تأیید دو مرحله‌ای (2-Step Verification)** را روشن کنید — بدون این، گزینه بعدی ظاهر نمی‌شود.
3. به [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords) بروید.
4. یک نام بنویسید (مثلاً `Supabase`) و **Create** بزنید.
5. یک رمز ۱۶ حرفی مثل `abcd efgh ijkl mnop` می‌گیرید. **فاصله‌ها را حذف کنید** → `abcdefghijklmnop`

### ب) وارد کردن در Supabase

از منوی سمت چپ: **Project Settings → Authentication → SMTP Settings**

گزینه **Enable Custom SMTP** را روشن کنید و این مقادیر را بگذارید:

| فیلد | مقدار |
|---|---|
| Host | `smtp.gmail.com` |
| Port | `465` |
| Username | ایمیل کامل شما، مثلاً `myshop@gmail.com` |
| Password | همان رمز ۱۶ حرفی بدون فاصله |
| Sender email | همان ایمیل جیمیل |
| Sender name | `چی داری؟` |

روی **Save** بزنید.

> **نکته درباره پورت:** اگر با ۴۶۵ کار نکرد، پورت `587` را امتحان کنید.
> جیمیل هر دو را پشتیبانی می‌کند (۴۶۵ برای SSL و ۵۸۷ برای STARTTLS).

> **محدودیت جیمیل:** حساب رایگان جیمیل روزانه حدود **۵۰۰ ایمیل** می‌فرستد.
> برای مصرف بیشتر، از سرویس‌های اختصاصی مثل Resend، SendGrid یا Brevo استفاده کنید
> (تنظیماتشان دقیقاً به همین شکل است، فقط Host و Username فرق می‌کند).

---

## ۵. تنظیمات احراز هویت

از منوی: **Authentication → Sign In / Providers → Email**

- **Enable Email provider** روشن باشد.
- **Confirm email** روشن باشد (تا کاربر مجبور شود ایمیلش را تأیید کند).

سپس از **Authentication → URL Configuration**:

- در **Redirect URLs** این مقدار را اضافه کنید:
  ```
  chidari://auth-callback
  ```

---

## ✅ آزمایش

> **وضعیت فعلی:** اتصال تست شد و ثبت‌نام از سمت سرور جواب داد
> (`confirmation_sent_at` پر شد). تنها کار باقی‌مانده، تنظیم SMTP مرحله ۴ است
> تا سقف روزانه ایمیل برداشته شود.


1. برنامه را اجرا کنید.
2. تب **حساب من** → **ورود یا ثبت‌نام با ایمیل**
3. روی **ثبت‌نام کنید** بزنید، نام و ایمیل و رمز (حداقل ۶ نویسه) وارد کنید.
4. باید صفحه «📬 ایمیل تأیید فرستادیم» را ببینید.
5. ایمیلتان را باز کنید و روی لینک تأیید بزنید.
6. به برنامه برگردید و وارد شوید.

در پنل Supabase از بخش **Authentication → Users** می‌توانید کاربران ثبت‌شده را ببینید.

---

## 🔧 رفع اشکال

| مشکل | راه‌حل |
|---|---|
| «ارسال ایمیل تأیید ناموفق بود» | تنظیمات SMTP را چک کنید؛ معمولاً رمز App Password اشتباه یا دارای فاصله است |
| ایمیل نمی‌رسد | پوشه **اسپم** را ببینید؛ همچنین در Supabase → Logs → Auth خطا را بررسی کنید |
| «تعداد درخواست‌ها زیاد بود» | سقف پیش‌فرض Supabase پر شده — SMTP سفارشی را فعال کنید |
| «این ایمیل قبلاً ثبت شده» | از **Authentication → Users** کاربر را حذف کنید یا وارد شوید |
| «اتصال به سرور تنظیم نشده» | مقادیر `URL` و `ANON_KEY` را در `SupabaseConfig.kt` پر نکرده‌اید |

---

## 📌 وضعیت فعلی داده‌ها

در این نسخه **احراز هویت** آنلاین است (Supabase) ولی **فروشگاه‌ها و محصولات** هنوز
در پایگاه داده محلی (Room) روی خود گوشی ذخیره می‌شوند.

یعنی حساب کاربری بین دستگاه‌ها مشترک است، ولی فروشگاهی که می‌سازید فعلاً فقط
روی همان گوشی دیده می‌شود. برای همگام‌سازی کامل، گام بعدی ساختن جدول‌های
`stores` و `products` در Supabase و افزودن یک `RemoteDataSource` است — ساختار
`AppRepository` از قبل برای این کار آماده شده است.
